- hosts: prod
  remote_user: vagrant
  serial: 1
  roles:
    - common-centos
    - docker-centos
    - docker-compose
    - consul-centos
    - registrator
    - consul-template
    - nginx-centos
    - filebeat
